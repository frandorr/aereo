---
phase: quick
plan: 260330
type: execute
wave: 1
depends_on: []
files_modified:
  - components/aer/repository/models.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Domain models contain only business-relevant fields, not implementation IDs
    - Repository implementations own UUID generation, not domain models
  artifacts:
    - path: components/aer/repository/models.py
      provides: Domain models without UUID implementation details
      contains: "class GridDefinition, class GridCell, class Asset, class Derivative"
  key_links: []
---

<objective>
Remove UUID fields from domain entities in models.py. UUIDs are concrete implementation details that should be managed by repository implementations, not domain models.

Purpose: Keep domain models pure and implementation-agnostic
Output: Clean domain models in models.py
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@components/aer/repository/models.py
@components/aer/repository/core.py
</context>

<tasks>

<task type="auto">
  <name>task 1: Remove UUID fields from domain models</name>
  <files>components/aer/repository/models.py</files>
  <action>
    Remove UUID fields from domain models that are implementation details:

    1. GridDefinition: Remove `definition_id: UUID` field
    2. GridCell: Remove `cell_id: UUID` and `definition_id: UUID` fields
    3. Asset: Remove `asset_id: UUID` field
    4. Derivative: Remove `derivative_id: UUID` and `cell_id: UUID` fields

    Keep the `UUID` import in case it's used elsewhere, or remove if no longer needed.
    The domain models should only contain business-relevant fields (names, coordinates, paths, timestamps, etc.)
  </action>
  <verify>
    <automated>python -c "from aer.repository.models import GridDefinition, GridCell, Asset, Derivative; print('Models import OK')"</automated>
  </verify>
  <done>Domain models contain no UUID fields - only business-relevant attributes remain</done>
</task>

</tasks>

<verification>
- Models import without errors
- No UUID fields remain in GridDefinition, GridCell, Asset, Derivative classes
- Business fields (name, coordinates, paths, timestamps) preserved
</verification>

<success_criteria>
Domain models in models.py contain only domain-relevant fields. UUID fields (definition_id, cell_id, asset_id, derivative_id) removed. Repository implementations will handle ID generation.
</success_criteria>

<output>
After completion, create `.planning/quick/260330-adapt-repository-and-models-to-remove-uu/260330-SUMMARY.md`
</output>
