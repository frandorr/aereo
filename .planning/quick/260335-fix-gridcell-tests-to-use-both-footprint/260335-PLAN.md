# Quick Task 260335: Fix GridCell tests to use both footprint and utm_footprint, add catalog component tests

**Mode:** quick
**Date:** 2026-03-31

## Plan

### Task 1: Fix GridCell test fixtures and instantiations
- **Files:** `test/components/aer/spatial/test_core.py`, `test/components/aer/repository/test_spatial_integration.py`, `components/aer/repository/spatial.py`
- **Action:** Update all GridCell instantiations to include both `footprint` (EPSG:4326) and `utm_footprint` (UTM CRS) as separate required attributes. Fix repository to pass `footprint` from the geometry column when creating GridCell objects.
- **Verify:** All spatial tests pass
- **Done:** When pytest passes for spatial test files

### Task 2: Add catalog component tests
- **Files:** `test/components/aer/catalog/test_core.py`
- **Action:** Write comprehensive tests for Product, AssetVariable, and Asset models including immutability, defaults, metadata, and integration tests
- **Verify:** All catalog tests pass
- **Done:** When pytest passes for catalog test file

## Summary

- Fixed `GridCell` tests to pass both `footprint` (EPSG:4326) and `utm_footprint` (UTM CRS) as separate attributes
- Updated `AerParquetSpatialRepository.get_grid_cells()` to include `footprint` from the geometry column
- Updated `test_footprint_property_matches_utm_footprint` to correctly assert they are different (different CRS)
- Added 21 comprehensive tests for catalog component (Product, AssetVariable, Asset)
- All 56 tests pass
