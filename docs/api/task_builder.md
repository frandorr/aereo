# Task builder

`aereo.task_builder` contains the pure-function logic that turns search results
into a sequence of `ExtractionTask` objects. Most users interact with it
indirectly through `AereoClient.prepare_tasks()`.

::: aereo.task_builder
