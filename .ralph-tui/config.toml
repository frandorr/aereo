# Ralph TUI Project Config — aer monorepo
tracker = "json"
agent = "kimi"
maxIterations = 10
iterationDelay = 1000
outputDir = ".ralph-output"

[agentOptions]
model = "kimi"

[errorHandling]
strategy = "skip"
maxRetries = 3
retryDelayMs = 5000
