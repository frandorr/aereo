# Install the local aereo package from the polylith workspace project.
# The path is relative to the repo root because repo2docker builds from there.
-e projects/aereo[all]

# Notebook runtime
jupyterlab>=4
ipykernel>=7

# Sensor-specific / example-specific extras used in notebooks but not part of aereo[all]
satpy>=0.59
scipy>=1.13
odc-loader>=0.6.4
